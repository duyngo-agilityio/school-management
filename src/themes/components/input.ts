import { defineStyleConfig } from '@chakra-ui/react';

export const Input = defineStyleConfig({
  baseStyle: {
    field: {
      color: 'textPrimary',
      fontSize: 'sm',
      border: '1px solid',
    },
  },
});
